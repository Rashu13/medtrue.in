server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # CORS headers for direct API access from browser
    # API calls are made directly to https://medtrue.cloud/api from the frontend
}
