version: '3.8'

services:
  # Backend API (.NET)
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Host=62.72.13.162;Port=5432;Database=postgres;Username=postgres;Password=UErqNKVZ53XMDsiUMibk482Mz7TvXXPqxwtZWiF0Vgd3UK2j2jYIrKy9xGegwkZy
    volumes:
      - medtrue-uploads:/app/wwwroot/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medtrue-network

  # Frontend (Vite + Nginx) - Serves on medtrue.cloud
  frontend:
    build:
      context: ./medtrue_admin
      dockerfile: Dockerfile
    # Port 80 is handled by Coolify Proxy, so we don't map it to Host to avoid "Port already allocated" error.
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medtrue-network

volumes:
  medtrue-uploads:

networks:
  medtrue-network:
    driver: bridge
